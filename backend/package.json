{
  "name": "micro-marketplace-backend",
  "version": "1.0.0",
  "description": "Backend for Micro Marketplace App (Node.js, Express, MongoDB, JWT)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node -e \"import('./src/app.js').then(()=>process.exit(0)).catch(err=>{console.error(err);process.exit(1);})\"",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1"
  }
}
